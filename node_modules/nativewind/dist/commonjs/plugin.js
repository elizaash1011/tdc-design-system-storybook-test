"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _plugin = _interopRequireDefault(require("tailwindcss/plugin"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function kebabCase(str) {
  return str.replace(/[A-Z]+(?![a-z])|[A-Z]/g, ($, ofs) => (ofs ? "-" : "") + $.toLowerCase());
}
const nativewind = _plugin.default.withOptions(() => api => {
  api.matchVariant("@map", (value = "", {
    modifier
  }) => {
    value = kebabCase(value.replace(/&/, "\\&"));
    if (modifier) {
      modifier = modifier.replace(/&/, "\\&");
    }

    // Keep mapping metadata in the declaration block through CSS optimization.
    const target = modifier && !value ? modifier : value;
    const source = modifier && value ? kebabCase(modifier) : "*";
    const property = source === "*" ? "-rn-native-mapping" : `-rn-native-mapping-${source}`;
    // matchVariant inserts its slot into the final selector inside the block.
    return target ? `& { ${property}: ${target}; & }` : "";
  }, {
    values: {
      DEFAULT: undefined
    }
  });
});
var _default = exports.default = nativewind;
//# sourceMappingURL=plugin.js.map