"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.borderInlineWidth = exports.borderBlockWidth = void 0;
var _constants = require("../constants.js");
function logicalWidth(start, end) {
  return (resolve, descriptor) => {
    const resolved = resolve(descriptor[2]);
    const values = Array.isArray(resolved) ? resolved : [resolved];
    if (values.length < 1 || values.length > 2 || !values.every(value => typeof value === "number" && Number.isFinite(value) && value >= 0)) return;
    return {
      [_constants.ShortHandSymbol]: true,
      [start]: values[0],
      [end]: values[1] ?? values[0]
    };
  };
}
const borderBlockWidth = exports.borderBlockWidth = logicalWidth("borderTopWidth", "borderBottomWidth");
const borderInlineWidth = exports.borderInlineWidth = logicalWidth("borderStartWidth", "borderEndWidth");
//# sourceMappingURL=logical-border-width.js.map