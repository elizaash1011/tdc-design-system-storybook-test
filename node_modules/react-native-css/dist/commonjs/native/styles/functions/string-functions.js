"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.join = exports.gradientPosition = exports.colorStop = void 0;
const gradientPosition = (resolveValue, value) => {
  const position = resolveValue(value[2]);
  return typeof position === "number" ? `${position}px` : position;
};
exports.gradientPosition = gradientPosition;
const colorStop = (resolveValue, value) => {
  const args = resolveValue(value[2]);
  if (!Array.isArray(args)) return args;
  const [color, position] = args;
  if (typeof color !== "string") return;
  if (position === undefined) return color;
  if (typeof position !== "number" && typeof position !== "string") return;
  return `${color} ${typeof position === "number" ? `${position}px` : position}`;
};
exports.colorStop = colorStop;
const join = (resolveValue, value) => {
  const args = resolveValue(value[2]);
  if (!Array.isArray(args)) {
    return args;
  }
  const array = args[0];
  const separator = args[1] ?? ",";
  if (!Array.isArray(array)) {
    return array;
  }
  if (!separator || typeof separator !== "string") {
    return array.join();
  }
  return array.join(separator);
};
exports.join = join;
//# sourceMappingURL=string-functions.js.map