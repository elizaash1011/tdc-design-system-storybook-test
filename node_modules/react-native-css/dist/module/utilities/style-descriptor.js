"use strict";

export function isStyleDescriptorArray(value) {
  return Array.isArray(value) && !isStyleFunction(value);
}
export function isStyleFunction(value) {
  if (!Array.isArray(value)) return false;
  const marker = value[0];
  return marker !== null && typeof marker === "object" && !Array.isArray(marker) && Object.keys(marker).length === 0 && typeof value[1] === "string";
}
//# sourceMappingURL=style-descriptor.js.map